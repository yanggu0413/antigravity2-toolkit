# Auto-detect text files and normalize line endings
* text=auto

# Enforce LF for shell and command scripts
*.sh text eol=lf
*.command text eol=lf
啟動工具箱.sh text eol=lf
啟動工具箱.command text eol=lf

# Enforce CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf
