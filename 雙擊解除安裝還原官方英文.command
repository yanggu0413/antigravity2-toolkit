#!/bin/bash
cd "$(dirname "$0")"

echo "======================================================"
echo " 正在解除安裝在地化，恢復官方原版 (macOS)..."
echo "======================================================"

node bin/ag-toolkit.js restore "$@"
